import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata = {
  title: "Ikwukao.dev — Backend Engineer & AI Builder",
  description: `Ikwukao.dev is the personal website of Ikwuka Okoye, 
                a passionate backend engineer and AI builder. 
                Explore my projects, experience, and get in 
                touch to collaborate on innovative tech solutions.
              `,
};

export default function RootLayout({ children }) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
